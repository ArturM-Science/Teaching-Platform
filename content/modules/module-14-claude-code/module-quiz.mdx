---
title: "Module Quiz — Claude Code"
module: "module-14-claude-code"
lesson: 100
slug: "module-quiz"
---

# Module 14 Quiz

Score 80% or higher to mark this module complete.

<ModuleQuiz
  moduleSlug="module-14-claude-code"
  passPercent={80}
  next="Module 14 complete — you've finished the course content!"
  questions={[
    {
      type: 'match',
      question: "Match each Claude Code feature to what it does.",
      pairs: [
        { left: "CLAUDE.md", right: "Persistent project rules and context loaded at the start of every session" },
        { left: "Plan mode", right: "Agent explores and proposes a plan you approve before any edits happen" },
        { left: "Hooks", right: "Shell commands that run automatically on harness events (tool call, stop, etc.)" },
        { left: "Routines", right: "Scheduled or event-triggered cloud agent runs that execute without you at the keyboard" }
      ]
    },
    {
      question: "Claude Code is best described as:",
      options: [
        { label: "A model that's better at coding than Claude" },
        { label: "A harness around the model — tools, permissions, context management, and a loop that lets the model work in your repo and terminal", correct: true },
        { label: "A VS Code theme" },
        { label: "A cloud IDE" }
      ],
      explanation: "The model provides reasoning; Claude Code provides the agent loop, file/shell tools, permission system, and context plumbing. Understanding the harness is how you steer it."
    },
    {
      question: "When should you use plan mode?",
      options: [
        { label: "For every request, always" },
        { label: "For larger or riskier changes — the agent explores and proposes a plan you approve before any edits happen", correct: true },
        { label: "Only for documentation tasks" },
        { label: "When you want faster output" }
      ],
      explanation: "Plan mode separates 'figure out what to do' from 'do it'. For multi-file or ambiguous work, reviewing the plan first is much cheaper than reviewing a wrong implementation."
    },
    {
      type: 'order',
      question: "Put the steps for configuring a safe Claude Code workflow in the correct order.",
      steps: [
        "Write CLAUDE.md with project conventions, build commands, and gotchas",
        "Configure permissions — allowlist safe read-only tools, gate risky ones",
        "Enable sandboxed execution for shell commands where possible",
        "Set up git checkpoint discipline — commit before and after each agent session",
        "Define a verification step the agent must pass (tests, lint) before reporting done"
      ]
    },
    {
      question: "The permission system's main design idea is:",
      options: [
        { label: "Trust the agent fully after the first approval" },
        { label: "Actions are gated by configurable rules — safe operations flow, risky ones require approval until you explicitly allowlist them", correct: true },
        { label: "Block everything always" },
        { label: "Permissions only apply to MCP tools" }
      ],
      explanation: "It's the human-in-the-loop pattern from Module 10 applied to coding: gate by consequence, expand autonomy deliberately as trust is earned, sandbox where possible."
    },
    {
      question: "What are routines in Claude Code?",
      options: [
        { label: "Daily standup reminders for humans" },
        { label: "Scheduled or triggered cloud automations — agent runs that execute on a cron schedule or event without you at the keyboard", correct: true },
        { label: "Keyboard macros" },
        { label: "A test framework" }
      ],
      explanation: "Routines move agent work from interactive to autonomous: nightly dependency checks, triggered triage, recurring reports — long-horizon autonomy with the safety lessons applied."
    }
  ]}
/>

---

[← Lab: Configure a Safe Claude Code Workflow](/modules/module-14-claude-code/lesson-07-lab)
