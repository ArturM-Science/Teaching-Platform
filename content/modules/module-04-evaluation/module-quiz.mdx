---
title: "Module Quiz — Evaluation"
module: "module-04-evaluation"
lesson: 100
slug: "module-quiz"
---

# Module 04 Quiz

Score 80% or higher to mark this module complete.

<ModuleQuiz
  moduleSlug="module-04-evaluation"
  passPercent={80}
  next="Module 04 complete — Module 05: Workflow Patterns is next."
  questions={[
    {
      question: "Why is evaluating LLM systems harder than evaluating traditional software?",
      options: [
        { label: "LLM APIs are poorly documented" },
        { label: "Outputs are non-deterministic and often have many acceptable answers, so exact-match assertions don't work", correct: true },
        { label: "There are no testing frameworks for Python" },
        { label: "Evaluation requires GPUs" }
      ],
      explanation: "assert output == expected breaks when the same input can yield different valid answers. You need datasets, metrics, and statistical thinking instead of single assertions."
    },
    {
      type: 'match',
      question: "Match each eval metric type to what it actually measures.",
      pairs: [
        { left: "Exact match", right: "Whether output is identical to a reference string" },
        { left: "LLM-as-judge score", right: "Holistic quality rated by another model using a rubric" },
        { left: "Human evaluation", right: "Ground-truth quality — expensive, slow, and the gold standard" },
        { left: "Task-based metric", right: "Whether the output achieves the downstream goal (e.g. tests pass)" }
      ]
    },
    {
      question: "Your agent scores 95% on 'response contains the keyword'. Why might this metric mislead you?",
      options: [
        { label: "95% is statistically impossible" },
        { label: "It measures keyword presence, not whether the answer is correct or useful — a proxy metric can diverge from the real goal", correct: true },
        { label: "Keyword checks are too expensive" },
        { label: "Metrics over 90% are always overfitting" }
      ],
      explanation: "Every metric is a proxy. 'Contains refund' passes 'I will never refund you.' Always ask: what am I actually trying to measure, and where does this proxy break?"
    },
    {
      type: 'order',
      question: "Put the steps for building a trustworthy eval harness in the correct order.",
      steps: [
        "Collect real or realistic user inputs including edge cases",
        "Define success criteria and metrics before building",
        "Run baseline eval on the current system",
        "Make a change (prompt, model, or logic)",
        "Run the full eval suite again and compare pass rates",
        "Promote or reject the change based on evidence"
      ]
    },
    {
      question: "Which is a documented bias of LLM-as-judge scoring?",
      options: [
        { label: "Preferring shorter answers in all cases" },
        { label: "Position bias — favouring the first (or last) of two compared answers regardless of quality", correct: true },
        { label: "Refusing to score more than 10 items" },
        { label: "Only working in English" }
      ],
      explanation: "Judges show position bias, verbosity bias (favouring longer answers), and self-preference. Mitigations: swap orders, use rubrics, calibrate against human labels."
    },
    {
      question: "In the lab's eval harness, why run the full eval suite before AND after a prompt change?",
      options: [
        { label: "To double the token spend" },
        { label: "To catch regressions — a change that fixes one case can silently break others", correct: true },
        { label: "Because the API requires warm-up calls" },
        { label: "To populate the cache" }
      ],
      explanation: "Prompt changes have non-local effects. The before/after comparison across the whole suite is what turns 'it feels better' into evidence."
    }
  ]}
/>

---

[← Lab: Eval Harness for the Research Agent](/modules/module-04-evaluation/lesson-06-lab)
