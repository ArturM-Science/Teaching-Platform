---
title: "Module Quiz — Security & Safety"
module: "module-08-security-safety"
lesson: 100
slug: "module-quiz"
---

# Module 08 Quiz

Score 80% or higher to mark this module complete.

<ModuleQuiz
  moduleSlug="module-08-security-safety"
  passPercent={80}
  next="Module 08 complete — Module 09: Observability, Cost & Reliability is next."
  questions={[
    {
      question: "What makes the LLM threat model fundamentally different from classic application security?",
      options: [
        { label: "LLM apps don't use databases" },
        { label: "Instructions and data travel in the same channel — any text the model reads can act as instructions", correct: true },
        { label: "Attackers need GPU access" },
        { label: "TLS doesn't work for LLM traffic" }
      ],
      explanation: "SQL injection was solved by separating code from data. LLMs have no such separation: a retrieved document, email, or webpage can carry instructions the model may follow."
    },
    {
      type: 'match',
      question: "Match each OWASP agentic risk to its core description.",
      pairs: [
        { left: "Prompt injection", right: "Malicious instructions embedded in content the model must read" },
        { left: "Excessive agency", right: "Agent has more permissions or autonomy than its task requires" },
        { left: "Insecure output handling", right: "Agent output is passed to another system without sanitisation" },
        { left: "Supply chain risk", right: "A tool or dependency the agent relies on is compromised" }
      ]
    },
    {
      question: "Why is prompt injection called 'the unsolvable problem'?",
      options: [
        { label: "Nobody has tried to fix it" },
        { label: "There's no reliable mechanism to make a model ignore instructions embedded in content it must read — defences reduce, not eliminate, the risk", correct: true },
        { label: "It only affects open-source models" },
        { label: "It's solved by lowering temperature" }
      ],
      explanation: "Delimiters, system-prompt warnings, and classifiers all help and all leak. You design assuming injection will sometimes succeed — and limit what success can do."
    },
    {
      type: 'order',
      question: "Put the defence-in-depth layers in order from innermost (closest to the model) to outermost (closest to the environment).",
      steps: [
        "System prompt: instructions and role definition",
        "Input classifiers: flag suspicious content before the model sees it",
        "Least-privilege tools: restrict what the agent can act on",
        "Human approval gates: require confirmation for irreversible actions",
        "Audit logging: record all actions for post-incident review"
      ]
    },
    {
      question: "Your agent must process documents from untrusted sources. The right architecture treats retrieved content as:",
      options: [
        { label: "Trusted, since your RAG pipeline fetched it" },
        { label: "Untrusted input — quarantined from high-privilege actions, e.g. the agent that reads it can't also execute sensitive tools", correct: true },
        { label: "System-prompt material" },
        { label: "Safe after keyword filtering" }
      ],
      explanation: "Provenance determines trust. Splitting 'reads untrusted content' from 'wields dangerous tools' (dual-LLM / quarantine patterns) keeps an injected document from pulling the trigger."
    },
    {
      question: "In the red-team lab, why attack your own agent before shipping?",
      options: [
        { label: "To generate marketing material" },
        { label: "To discover which injections and abuses actually work on YOUR system, so hardening targets real holes instead of guesses", correct: true },
        { label: "Because OWASP requires a certificate" },
        { label: "To train the model on the attacks" }
      ],
      explanation: "Generic checklists miss system-specific holes. Red-teaming your own agent — injections in retrieved docs, tool-abuse chains, data exfiltration paths — produces the fix list that matters."
    }
  ]}
/>

---

[← Lab: Red-team and Harden Your Agent](/modules/module-08-security-safety/lesson-06-lab)
