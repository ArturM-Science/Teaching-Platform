---
title: "Module Quiz — Multi-Agent Systems"
module: "module-06-multi-agent-systems"
lesson: 100
slug: "module-quiz"
---

# Module 06 Quiz

Score 80% or higher to mark this module complete.

<ModuleQuiz
  moduleSlug="module-06-multi-agent-systems"
  passPercent={80}
  next="Module 06 complete — Module 07: Deployment & Serving is next."
  questions={[
    {
      type: 'match',
      question: "Match each agent role to its primary responsibility.",
      pairs: [
        { left: "Orchestrator", right: "Decomposes the task, delegates to subagents, and integrates results" },
        { left: "Specialist", right: "Executes a focused domain task with deep context for that area" },
        { left: "Critic", right: "Reviews output from a fresh context to catch errors the producer is blind to" },
        { left: "Subagent", right: "Receives a delegated subtask and returns a structured result" }
      ]
    },
    {
      question: "What actually makes a system multi-agent, rather than a workflow with several LLM calls?",
      options: [
        { label: "Using more than one model provider" },
        { label: "Multiple agents with their own context, role, and decision-making, coordinating with each other — not just fixed steps in your code", correct: true },
        { label: "Having more than three prompts" },
        { label: "Running on more than one server" }
      ],
      explanation: "A workflow's control flow lives in your code; agents decide. Multi-agent means several deciding entities with separate contexts coordinating — which buys flexibility and new failure modes."
    },
    {
      question: "Why add a critic agent instead of letting each agent check its own work?",
      options: [
        { label: "Critics are cheaper to run" },
        { label: "A separate context isn't anchored on the producer's reasoning, so it catches errors the producer is blind to", correct: true },
        { label: "Self-checking is technically impossible" },
        { label: "Critics can use tools and producers can't" }
      ],
      explanation: "A model reviewing its own output inherits its own assumptions. A critic with fresh context and an adversarial brief breaks that anchoring — same idea as human code review."
    },
    {
      type: 'order',
      question: "Put the steps for designing a multi-agent system in the right order.",
      steps: [
        "Define the overall goal and verify a single agent can't handle it reliably",
        "Identify the distinct roles and what each produces",
        "Choose a communication pattern (sequential, blackboard, event-driven)",
        "Define trust boundaries and what each agent is allowed to write",
        "Plan failure mitigations — what happens if one agent goes wrong",
        "Measure total token cost and compare to single-agent baseline"
      ]
    },
    {
      question: "What is memory poisoning in a multi-agent system?",
      options: [
        { label: "A vector database running out of disk" },
        { label: "One agent writing wrong or malicious content into shared state, which other agents then trust and build on", correct: true },
        { label: "Two agents using the same API key" },
        { label: "Forgetting to clear the context between sessions" }
      ],
      explanation: "Shared memory is a trust boundary. One bad write propagates: downstream agents treat it as ground truth. Mitigations include provenance tracking and validating before writing."
    },
    {
      question: "In the cross-system lab, why put a strict Pydantic schema at the boundary between System A (LangGraph) and System B (CrewAI)?",
      options: [
        { label: "Both frameworks require Pydantic internally" },
        { label: "The boundary is a trust edge — validating the payload means System B never ingests malformed or unexpected data from System A", correct: true },
        { label: "It makes the systems run in parallel" },
        { label: "Pydantic encrypts the data in transit" }
      ],
      explanation: "Different systems, different owners, different failure modes. A validated contract at the boundary means failures are caught at the edge, with a clear error, instead of corrupting System B's run."
    }
  ]}
/>

---

[← Lab 2: Cross-System Agent Communication](/modules/module-06-multi-agent-systems/lesson-11-lab-2)
