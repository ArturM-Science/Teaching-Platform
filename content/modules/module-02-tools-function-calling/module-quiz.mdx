---
title: "Module Quiz — Tools & Function Calling"
module: "module-02-tools-function-calling"
lesson: 100
slug: "module-quiz"
---

# Module 02 Quiz

Score 80% or higher to mark this module complete.

<ModuleQuiz
  moduleSlug="module-02-tools-function-calling"
  passPercent={80}
  next="Module 02 complete — Module 03: Memory & Knowledge is next."
  questions={[
    {
      question: "Why do tool names and descriptions matter so much?",
      options: [
        { label: "They're checked by the API for uniqueness" },
        { label: "They are the only information the model has when deciding which tool to call and how", correct: true },
        { label: "They affect billing" },
        { label: "They're displayed to end users" }
      ],
      explanation: "The model picks tools purely from names, descriptions, and schemas in its context. A vague description means wrong or missed tool calls — tool design is prompt design."
    },
    {
      type: 'match',
      question: "Match each function-calling step to who or what performs it.",
      pairs: [
        { left: "Selects the tool name and generates arguments", right: "The model" },
        { left: "Executes the function code", right: "Your application code" },
        { left: "Validates arguments against the schema", right: "Your application code (or the SDK)" },
        { left: "Decides whether to call another tool or finish", right: "The model" }
      ]
    },
    {
      question: "What problem does the Model Context Protocol (MCP) solve?",
      options: [
        { label: "It makes models faster" },
        { label: "It standardises how tools and data sources are exposed, so any MCP client can use any MCP server without custom integration", correct: true },
        { label: "It compresses the context window" },
        { label: "It replaces function calling entirely" }
      ],
      explanation: "MCP is a standard interface between agents and tool/data providers — write a server once, and every MCP-compatible client (Claude Code, IDEs, etc.) can use it."
    },
    {
      question: "A tool call times out. What should a well-designed agent do?",
      options: [
        { label: "Crash, since the task can't continue" },
        { label: "Return a structured error message to the model so it can retry, pick another tool, or tell the user", correct: true },
        { label: "Silently return an empty string" },
        { label: "Retry forever until it succeeds" }
      ],
      explanation: "Tool errors should flow back to the model as information, not crash the loop or vanish. The model can often recover — but only if it's told what failed."
    },
    {
      type: 'order',
      question: "Put the steps for hardening a tool against misuse in the right order.",
      steps: [
        "Define a strict schema with required fields, enums, and descriptions",
        "Validate incoming arguments before executing",
        "Sandbox execution so damage from bad arguments is contained",
        "Return structured error information if execution fails",
        "Log inputs and outputs for auditability"
      ]
    },
    {
      question: "The model keeps calling a tool with arguments that fail schema validation. The best first fix is:",
      options: [
        { label: "Switch to a bigger model immediately" },
        { label: "Improve the parameter descriptions and add examples to the tool schema", correct: true },
        { label: "Remove validation so calls succeed" },
        { label: "Lower the temperature to exactly 0.31" }
      ],
      explanation: "Most malformed calls trace back to ambiguous schemas. Clarify descriptions, constrain types/enums, add examples — removing validation just moves the failure downstream."
    }
  ]}
/>

---

[← Lab: Multi-Tool Agent with Sandboxed Code Runner](/modules/module-02-tools-function-calling/lesson-05-lab)
